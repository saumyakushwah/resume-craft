import CustomStepper from "@/components/common/CustomStepper";
import { Box } from "@mantine/core";

export default function Home() {  
  return (
    <Box maw={1200} mx="auto" mt={100}>
      <CustomStepper />
    </Box>
  );
}